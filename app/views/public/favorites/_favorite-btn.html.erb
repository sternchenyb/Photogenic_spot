<div id="favorite-<%= spot.id %>">
  <% if spot.favorited_by?(current_user) %>
   <%= link_to favorite_path(spot), method: :delete, remote: true do %>
    <span class="fas fa-heart" style="color:red;"></span>
   <%= spot.favorites.count %>
   <% end %>
  <% else %>
    <%= link_to favorites_path(spot), method: :post, remote: true do %>
      <span class="fas fa-heart" style="color:red;"></span>
    <%= spot.favorites.count %>
    <% end %>
  <% end %>
</div>