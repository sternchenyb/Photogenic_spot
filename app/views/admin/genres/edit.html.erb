<div class='container'>
  <div class='row'>
    <div class='text-center'>
     <h5 class="text-dark bg-light p-3 mb-0 text-center">ジャンル編集</h5></br>
       ジャンル名
       <%= form_with model: [:admin,@genre],  method: :patch do |f| %>
       <%= f.text_field :name %>
       <%= f.submit '変更を保存', class: 'btn btn-success rounded-pill'%>
       <% end %>
      </div>
    </div>
  </div>
</div>